# Dependencies
/node_modules

# Next.js build output
/.next
/out

# Misc build/cache
*.tsbuildinfo

# Local env files (never commit secrets; .env.example is the template)
.env
.env.local
.env.*.local

# OS / editor noise
.DS_Store
*.log

# Local agent settings
.claude/settings.local.json

# Marketing source material (archive outside the product repo)
starmethod.pdf
.codex-starmethod-pages/
videos/

# Supabase local stack
supabase/.temp
